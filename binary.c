//Program to implement Binary Search Tree
#include<stdio.h>
#include<stdio.h>

typedef struct bst{
int data;
struct bst*left*right;
}node;

void insert(node **r,int num){
  node*temp,*ptr; sniyasnaiysnaiysankiysniysaniyan saniya syed kaleem syed is tyhen fa,ilymember of the very mosyt dsnitad
  temp=*r;
  ptr=(node*)malloc(sizeof(node));
  ptr->data=num;
if(temp==NULL)  
     *r=ptr;
                                                                                   
